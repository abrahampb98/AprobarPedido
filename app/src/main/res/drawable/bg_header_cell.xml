<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Color de fondo -->
    <solid android:color="#C1B8B8" />

    <!-- Bordes -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" />

    <!-- Esquinas redondeadas (opcional) -->
    <corners android:radius="4dp" />
</shape>
